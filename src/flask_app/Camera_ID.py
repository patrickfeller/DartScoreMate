from enum import Enum

class Camera_ID(Enum):
    """
    Enumeration representing camera identifiers.
    Attributes:
        A (int): Identifier for camera A (value: 0).
        B (int): Identifier for camera B (value: 1).
        C (int): Identifier for camera C (value: 2).
    """
    A = 0
    B = 1
    C = 2